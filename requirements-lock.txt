# 本番依存（サブ依存含むロックファイル）
# requirements.txt のパッケージ＋その推移的依存を厳密ピン留め
# Dependabot は requirements.txt を監視し、更新時にこのファイルも再生成する
#
# 再生成コマンド:
#   pip install -r requirements.txt && pip freeze > requirements-lock.txt
#   （手動でdev/ツール系を除外すること）

# --- トップレベル ---
Flask==3.1.3
requests==2.32.5
python-dotenv==1.2.1
Pillow==12.1.1
cachetools==5.5.2
redis==7.2.0

# --- Flask のサブ依存 ---
Werkzeug==3.1.6
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.1
blinker==1.9.0
MarkupSafe==3.0.3

# --- requests のサブ依存 ---
urllib3==2.6.3
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11
